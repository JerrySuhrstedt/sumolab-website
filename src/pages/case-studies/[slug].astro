---
import Layout from "../../layouts/Layout.astro";
import Section from "../../components/Section.astro";
import Eyebrow from "../../components/Eyebrow.astro";
import PrimaryCTA from "../../components/PrimaryCTA.astro";
import { caseStudies } from "../../data/case-studies";
import { founder } from "../../data/founder";

export function getStaticPaths() {
	return caseStudies.map((caseStudy) => ({
		params: { slug: caseStudy.slug },
		props: { caseStudy },
	}));
}

const { caseStudy } = Astro.props;

function truncate(text: string, max: number) {
	if (text.length <= max) return text;
	const cut = text.slice(0, max);
	return `${cut.slice(0, cut.lastIndexOf(" "))}...`;
}

const suffix = " | SumoLab Case Studies";
const pageTitle =
	caseStudy.title.length + suffix.length <= 60 ? `${caseStudy.title}${suffix}` : truncate(caseStudy.title, 60);
const metaDescription = truncate(caseStudy.summary, 155);

const structuredData = {
	"@context": "https://schema.org",
	"@type": "Article",
	headline: caseStudy.title,
	description: caseStudy.summary,
	about: caseStudy.client,
	author: {
		"@type": "Person",
		name: founder.name,
	},
	publisher: {
		"@type": "Organization",
		name: "SumoLab",
		url: "https://sumolab.co",
	},
	mainEntityOfPage: {
		"@type": "WebPage",
		"@id": `https://sumolab.co/case-studies/${caseStudy.slug}/`,
	},
};
---

<Layout title={pageTitle} description={metaDescription} structuredData={structuredData}>
	<main>
		<Section tone="offwhite" class="pt-14 pb-16 md:pt-20">
			<Eyebrow>Case Study &middot; {caseStudy.client}</Eyebrow>
			<h1 class="text-statement mt-4 max-w-3xl text-mega text-brand-ink">{caseStudy.title}</h1>
			<p class="text-supporting mt-6 max-w-2xl text-lg text-brand-neutral">{caseStudy.summary}</p>
		</Section>

		<Section tone="white">
			<div class="grid grid-cols-1 gap-12 lg:grid-cols-12 lg:gap-8">
				<div class="lg:col-span-4">
					<span class="text-label text-brand-neutral">Problem</span>
				</div>
				<div class="lg:col-span-8">
					<ul class="flex flex-col gap-4">
						{
							caseStudy.problem.map((line) => (
								<li class="text-supporting text-lg text-brand-ink">{line}</li>
							))
						}
					</ul>
				</div>
			</div>
		</Section>

		<Section tone="offwhite">
			<div class="grid grid-cols-1 gap-12 lg:grid-cols-12 lg:gap-8">
				<div class="lg:col-span-4">
					<span class="text-label text-brand-neutral">Action</span>
				</div>
				<div class="lg:col-span-8">
					<ul class="flex flex-col gap-4">
						{
							caseStudy.action.map((line) => (
								<li class="text-supporting text-lg text-brand-ink">{line}</li>
							))
						}
					</ul>
				</div>
			</div>
		</Section>

		<Section tone="ink">
			<div class="grid grid-cols-1 gap-12 lg:grid-cols-12 lg:gap-8">
				<div class="lg:col-span-4">
					<span class="text-label text-brand-citrus">Result</span>
				</div>
				<div class="lg:col-span-8">
					<ul class="flex flex-col gap-4">
						{
							caseStudy.result.map((line) => (
								<li class="text-supporting text-lg text-brand-offwhite/90">{line}</li>
							))
						}
					</ul>
				</div>
			</div>
		</Section>

		{
			caseStudy.keyTakeaways && (
				<Section tone="white">
					<Eyebrow>Key takeaways</Eyebrow>
					<h2 class="text-statement mt-4 max-w-2xl text-huge text-brand-ink">What this proved.</h2>
					<ul class="mt-10 grid grid-cols-1 gap-x-8 gap-y-5 sm:grid-cols-2">
						{caseStudy.keyTakeaways.map((item) => (
							<li class="flex items-start gap-3">
								<span class="mt-2 h-1.5 w-1.5 shrink-0 rounded-full bg-brand-purple" aria-hidden="true" />
								<span class="text-supporting text-brand-ink">{item}</span>
							</li>
						))}
					</ul>
				</Section>
			)
		}

		<Section tone="purple">
			<div class="flex flex-col items-start gap-6 lg:flex-row lg:items-center lg:justify-between">
				<h2 class="text-statement max-w-xl text-huge text-brand-white">Want results like these for your business?</h2>
				<PrimaryCTA tone="citrus" ring="citrus" />
			</div>
		</Section>
	</main>
</Layout>
